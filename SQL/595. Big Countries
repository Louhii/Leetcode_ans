-- Write your PostgreSQL query statement below
SELECT name, population, area FROM World
WHERE area >= 3000000
OR population >= 25000000

Due to question formatting, operation needs to include equals to to satisfy all test cases
